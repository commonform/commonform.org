<!DOCTYPE html>
<html lang="en-US">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title %> <%= edition %></title>
    <link rel=stylesheet href=/normalize.css> <link rel=stylesheet
    href=/fonts.css> <link rel=stylesheet href=/styles.css>
  </head>
  <body>
    <header role="banner">
      <a href=/><img alt="Common Form logotype" src=/logo.svg></a>
      <h1><%= title %> </h1>
      <p class=edition><%= spelled %></p>
      <p class=digest><code class=digest><%= digest %></code></p>
    </header>
    <main role="main">
      <table>
        <tr>
          <th>Publisher</th>
          <td><a href="/<%= publisher %>"><%= publisher %></a></td>
        </tr>
        <tr>
          <th>Project</th>
          <td><a href="/<%= publisher %>/<%= project %>"><%= project %></a></td>
        </tr>
        <tr>
          <th>Type</th>
          <td><%= projectMetadata.buildingBlock ? 'Building Block' : 'Complete Form' %></td>
        </tr>
        <tr>
          <th>Published</th>
          <td><%= published %></td>
        </tr>
        <tr>
          <th>Downloads</th>
          <td>
            <ul>
              <li><a download="<%= publisher %> <%= project %> <%= edition %>.docx" href="<%= docx %>">Microsoft Word</a></li>
              <li><a download="<%= publisher %> <%= project %> <%= edition %>.json" href="<%= json %>">JSON Data</a></li>
              <li><a download="<%= publisher %> <%= project %> <%= edition %>.md" href="<%= markdown %>">Markdown</a></li>
            </ul>
          </td>
        </tr>
        <% if (notes) { %>
        <tr>
          <th>Release Notes</th>
          <td>
            <ul>
              <% notes.forEach((note) => { %>
              <li><%= note %></li>
              <% }) %>
            </ul>
          </td>
        </tr>
        <% } %>
      </table>
      <%- rendered %>
    </main>
    <footer role="contentinfo">
      <a href="<%= github %>">Source file on GitHub.</a>
    </footer>
  </body>
</html>
