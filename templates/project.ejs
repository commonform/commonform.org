<!DOCTYPE html>
<html lang=en-US>
  <head>
    <meta charset=UTF-8>
    <meta name=viewport content="width=device-width, initial-scale=1" />
    <title><%= publisher %>’s <%= project %></title>
    <link rel=stylesheet href=/normalize.css>
    <link rel=stylesheet href=/fonts.css>
    <link rel=stylesheet href=/styles.css>
  </head>
  <body>
    <header role=banner>
      <a href=/><img alt="Common Form logotype" src=/logo.svg></a>
      <h1>Common Form</h1>
      <p>a free, open repository of legal forms</p>
    </header>
    <main role=main>
      <h2><%= project %></h2>
      <table>
        <tr>
          <th>Publisher</th>
          <td><a href="/<%= publisher %>"><%= publisher %></a></td>
        </tr>
        <tr>
          <th>Type</th>
          <td><%= buildingBlock ? 'Building Block ' : 'Complete Form'%></td>
        </tr>
      </table>
      <%- description %>
      <ul>
        <% editions.forEach((edition) => { %>
        <li>
          <a href=./<%= edition.number %>><%= edition.spelled %></a>
          (<%= edition.published %>)
        </li>
      <% }) %>
      </ul>
    </main>
    <footer role=contentinfo>
      <p><a href=/guide#editions>What's with the edition numbers?</a></p>
    </footer>
  </body>
</html>
